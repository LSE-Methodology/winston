# Winston HPC User Guide

This repo contains the documentation site for the Winston HPC cluster.

## Quick start (local preview)

```bash
python -m venv .venv
source .venv/bin/activate
pip install mkdocs-material
mkdocs serve
```
